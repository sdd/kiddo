name: CI / MSRV

on:
  push:
    branches:
      - master
      - v5.x.x
  pull_request:
    branches:
      - "**"
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  MSRV_RUST_VERSION: "TBD"

jobs:
  lint-msrv:
    name: Lint (MSRV)
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
        with:
          show-progress: false

      - name: Get MSRV
        run: |
          echo "MSRV_RUST_VERSION=$(grep '^rust-version' Cargo.toml | sed 's/.*= *"\(.*\)".*/\1/')" >> "$GITHUB_ENV"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Cache Toolchain
        uses: actions/cache@v6
        with:
          path: ~/.rustup
          key: toolchain-x86-64-${{ env.MSRV_RUST_VERSION }}

      - name: Install MSRV Toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: clippy
          toolchain: ${{ env.MSRV_RUST_VERSION }}

      - name: Prepare example artifacts
        run: bash .github/scripts/prepare-example-artifacts.sh

      - name: Run Clippy
        run: cargo clippy --features=serde,rkyv_08,test_utils --no-deps

      - name: Install cargo-hack
        uses: baptiste0928/cargo-install@v3
        with:
          crate: cargo-hack
          version: "^0.6"

      - name: Cargo check all targets and features (MSRV)
        run: cargo hack check --workspace --each-feature --all-targets --exclude-features simd

  test-msrv:
    name: Test (MSRV)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: write

    steps:
      - uses: actions/checkout@v7
        with:
          show-progress: false

      - name: Get MSRV
        run: |
          echo "MSRV_RUST_VERSION=$(grep '^rust-version' Cargo.toml | sed 's/.*= *"\(.*\)".*/\1/')" >> "$GITHUB_ENV"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Cache Toolchain
        uses: actions/cache@v6
        with:
          path: ~/.rustup
          key: toolchain-x86-64-${{ env.MSRV_RUST_VERSION }}

      - name: Install MSRV Toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: clippy
          toolchain: ${{ env.MSRV_RUST_VERSION }}

      - name: Prepare example artifacts
        run: bash .github/scripts/prepare-example-artifacts.sh

      - name: Install cargo-nextest
        uses: taiki-e/install-action@nextest

      - name: Cargo nextest
        run: |
          cargo nextest run --workspace --features=serde,rkyv_08,test_utils

      - name: Cargo test (Doctests)
        run: |
          cargo test --workspace --doc --features=serde,rkyv_08,test_utils
